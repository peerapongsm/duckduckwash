# DuckDuckWash — Memory Log
**Date:** 2026-06-12 | **Last updated:** 2026-06-18 | **Status: v1.0.5 built + launch-verified on branch `fix/v1.0.5-startup-crash-chart-updater`; awaiting GitHub repo owner to publish** (aunt has v1.0.0; v1.0.1–v1.0.4 never worked/sent — v1.0.4 crashed on launch, see below)

---

## 1. Project Overview
- **App name: DuckDuckWash.** Pet project for the user's aunt, who owns a local laundry care business.
- Goal: replace her Excel-based workflow (orders, billing, customers) which is inefficient. Excel files in repo root are feature reference only — too messy to import.
- Primary user is the aunt (non-technical, elderly) — UX must be easy for low-tech and elderly users: big buttons, minimal fields, no jargon.
- **English-only UI.** (Earlier Thai/English toggle idea was scrapped 2026-06-12 — develop in English only.)
- Branding: single source file `icon/duckduckwash.png` (now alt duck art, 1024x1024 square — swapped from old 488x512 art, commit `e8dc379`; original alt kept at `icon/alt-duckduckwash.png`). Derived copies: `src/renderer/src/assets/logo.png` (in-app), `resources/icon.png` (window), `build/icon.png` (installer; square already, no padding needed).

## 2. Key Decisions
- **Deployment: fully local, single Windows PC.** No customer-facing site, no cloud. Ship NSIS installer directly to aunt.
- **Stack: Electron + React + TypeScript + SQLite** (better-sqlite3, WAL) + **Tailwind CSS + daisyUI**. Renderer talks to main only via typed IPC.
- **No data import — start fresh.**
- **Business-only tracking** — exclude aunt's personal expenses.
- **Cash-only store.** No payment-method field.
- Git is local-only version control; `.gitignore` blocks `*.xlsx` (aunt's real financial data), `*.db`, `backups/`, `.~lock.*#`.

### Business flow (defined by user 2026-06-12)
1. Customer walks in, drops off load.
2. Aunt creates order at drop-off with just: customer name/contact + which service types they want. Customer leaves.
3. Later she edits the order: kg, garment checklist (shirt, dress, skirt, blouse, …) with count per type, per-garment flag `special_care` only (deliberate simplification: a boolean instead of fabric types). **No iron flag on garments** — ironing is already expressed by the service categories [wash/dry/fold/iron] and [iron].
4. **Order status flow: `waiting_input` → `in_progress` → `complete` → `closed`.** New order starts at waiting_input; after detail input → in_progress; finished washing → complete; customer picked up AND paid → closed.
- Garment checklist is informational (anti-forget/anti-dispute); price comes from services, not garments. **But it is required at detail input** — aunt must specify how many shirts/dresses/blouses etc. the order contains (≥1 garment row) before details can be saved.

### Price model (final 2026-06-12 — exactly 4 service categories, bedding dropped)
| service | unit | price |
|---|---|---|
| wash/dry/fold | kg | 150 |
| wash/dry/fold/iron | kg | 200 |
| iron | item | custom |
| dry clean | item | custom |

- **Delivery is free (changed post-v1.0.0, commit `4704cf8`)** — `is_delivery` is an informational tag only; no fee in totals, `delivery_fee` setting no longer seeded/used. Old closed orders keep their +20 totals (historical).

At order creation aunt only picks categories; later she fills in kg (per-kg services) or the custom price (iron, dry clean).
- **Service category is single-select (2026-06-12, post-v1.0.1):** one category per order; clicking another replaces selection, clicking selected deselects. IPC still takes `service_ids` array (now length 1) — no backend change.

- **Walk-ins are never added to the customers table.** Orders store `customer_name`/`customer_location` inline; nullable `customer_id` only when a saved regular is picked. Regulars created deliberately on Customers screen only (most walk-ins are foreign travelers).
- **Customer names are not unique** (3 Peters OK). Disambiguate in UI via location/phone/last-order date.
- **Orders store full datetime (`created_at`)** — name + location + datetime is the practical order identifier.
- Roles: now owner-only, no login. Future: admin + 1 shared assistant account (Reports hidden), PIN gate, no full user management.

## 3. Current State
- **App fully implemented and shipped to installer** (2026-06-12), branch `feat/duckduckwash-app`, all 14 plan tasks executed subagent-driven. Per-task details live in git history of that branch.
- Verification: 10/10 vitest green, `tsc --noEmit` clean (node+web), `npm run build` clean, installer built at `release/DuckDuckWash Setup 1.0.0.exe` (~97MB NSIS, oneClick, `deleteAppDataOnUninstall: false` so DB survives uninstall).
- Final integration review fixes (commit `0c002e8`): advanceStatus takes `from` status (double-click guard), alert() on mutation errors, `saving` double-submit guards, `updatePrice` rejects <=0, WAL-safe backups via `db.backup()` (openDb before startup backup), saveDetails rejects closed orders / empty garments.
- **ABI seesaw:** `npm test` needs node ABI (`npm run rebuild:node`); `npm run dev` needs Electron ABI (`npm run rebuild:electron`); `build:win` auto-rebuilds for Electron.
- `build:win` now exits 0 cleanly (`publish: null` in electron-builder.yml fixed the old provider crash; `win.icon: build/icon.png` set explicitly).
- Merged to `main` (fast-forward) 2026-06-12; feature branch deleted; tests re-verified green on main.
- Post-merge UX round (commit `ef06e97`): THB unit labels in Settings; phone fields validate `^\d{10}$` when non-empty (intake + customers); garment "Other" replaced by free-text input — names persist in `order_garments` and resurface as preset buttons via new `garments:types` IPC (distinct query, no new table).
- UI redesign (commit `de20fdb`, frontend-design skill): custom daisyUI theme `duckwash` — duck-yellow primary #FFC93C, wash-blue secondary #4FA8D8, cream base #FFFBF2; Fredoka (display) + Nunito (body) bundled offline via @fontsource; left sidebar nav with emoji icons replaced bottom nav; status-colored left-edge order cards (warning/secondary/success/neutral); `rise` stagger animation + `lift` hover classes in main.css.
- Reports are date-range based (commit `92bbc15`): `rangeReport(db, from, to)` replaced monthlyReport; presets Today / This month / This year + free from/to pickers; buckets daily for spans ≤62 days, monthly beyond (annual readable); IPC `reports:range`.
- Delivery is editable on Order Details (commit `5b09c5b`): toggle persists via `saveDetails.is_delivery`, total recomputed with/without the 20 fee; complete-status rows also show Edit (customer picks up instead of delivery).
- **SHIPPED 2026-06-12:** installer `release/DuckDuckWash Setup 1.0.0.exe` (96.4MB, 15:35, SHA256 02E499CF…) sent to aunt — duckwash theme, range reports, editable delivery, desktop + start menu shortcuts.
- **Icon swapped to alt duck art post-ship** (commit `e8dc379`): installer rebuilt 16:16 (~105MB) with new icon.
- **Feedback round 1 (commit `4704cf8`):** phone → contact everywhere (free text, maxLength 256, 10-digit validation dropped; DB columns renamed `customers.contact` / `orders.customer_contact` with auto ALTER TABLE migration for v1.0.0 DBs — migration covered by test); expense categories now supplies/utilities/rent/food/salary/other; expense modal does batch entry (rows + shared date, `expenses:createMany` transaction, replaced `expenses:create`); delivery free (see price model). 12/12 tests + typecheck green.
- **v1.0.1 installer ready to send** (16:42, 100.6MB, SHA256 C71DDA62…) at `release/DuckDuckWash Setup 1.0.1.exe` — new duck icon + all round-1 changes; version bumped commit `11c80f2`. Stale `Setup 1.0.0.exe` builds still in `release/` (gitignored). Dev server stopped.
- **v1.0.2 built** (commit `716bb24`, installer 20:54, 100.6MB, SHA256 37B780BB…) at `release/DuckDuckWash Setup 1.0.2.exe` — NewOrder service buttons single-select; OrderDetails garment preset/custom add increments existing row on case-insensitive name match instead of spawning duplicate. Supersedes v1.0.1 (never sent). Auto-update declined — user keeps sending installers manually (running new Setup.exe updates in place, DB survives).
- **v1.0.3 built 2026-06-13** (installer 100.6MB, SHA256 62AA9F5F…) at `release/DuckDuckWash Setup 1.0.3.exe`, version bumped in package.json. Two features:
  1. **Backdate orders** — NewOrder has a date picker (📅, `max=today`, defaults to today). `OrderIntake.created_at` added; intake IPC inserts `${date} 12:00:00` via `COALESCE(?, datetime('now','localtime'))`. Only sent when picked day ≠ today, so normal same-day orders keep their real timestamp. Lets aunt enter old orders from her previous system.
  2. **Per-garment wearer** — `order_garments.wearer TEXT NOT NULL DEFAULT 'female'` (new column + ALTER TABLE `addColumn` migration in db.ts for old DBs, default 'female'). `Wearer = 'male'|'female'|'child'` type; dropdown (`select`) next to the special-care checkbox in OrderDetails; saveDetails persists it (whitelist-validated). New garment rows default 'female'.
  - Verified: 13/13 vitest green (added db migration test for wearer column), typecheck clean, build:win clean.
  3. **Wearer sections collapsible** (OrderDetails) — each wearer (male/female/child) is a daisyUI `<details>` collapse; Male open by default, others closed. Title shows a `badge-primary` with total garment count for that wearer so collapsed sections still show how many. Typecheck clean. (installer not yet rebuilt for this change)
  4. **Per-wearer garment presets from spreadsheet** (OrderDetails) — replaced the single shared `GARMENT_PRESETS` list with `WEARER_PRESETS: Record<Wearer,string[]>` prefilled from aunt's `PEE cloth type.xlsx` (columns MAN→male, LADY→female, KID UNISEX→child). Each wearer section now shows its own clothing types. Fixed source typos SHIRS-S→SHIRTS-S, TISHIRTS-L→TSHIRTS-L. Bedding (BEDSHEET/BLANKET/DUVET/PILLOW CASE/BLANKET COVER) + TOWEL-L/M/S kept under male as in the sheet (column A). Non-garment rows (contact/hotel names/services) skipped. Custom-added + legacy-DB garment names now live in `extraTypes` state (shown under every wearer) via `addExtras`; render builds per-wearer `rows` = presets + extras + any saved cell garment for that wearer. 13/13 test + typecheck green.
  5. **Reports → Export .xlsx** — new `exceljs` dependency (added to `dependencies`, ~+4MB packaged; externalized by electron-vite, packed into app.asar — 562 entries verified). `src/main/logic/reportExport.ts` `buildReportWorkbook(db,from,to)` builds 3 sheets: Summary (from/to/revenue/expenses/profit), Orders (date/customer/location/services/delivery/status/total), Expenses (date/category/description/amount). IPC `reports:export` shows `dialog.showSaveDialog` (default name `DuckDuckWash report {from} to {to}.xlsx`) then writes via `fs/promises.writeFile`; returns saved path or null if canceled. preload `reports.export`; Reports.tsx has a 📄 Export .xlsx button (disabled while no report / error / exporting), alerts the saved path. exceljs smoke-tested (writes valid PK-zip xlsx). Range/queries reuse `rangeReport` + `date(created_at) BETWEEN`.
- **v1.0.3 installer REBUILT 2026-06-13 16:26** (104.8MB, SHA256 A3DBD155…) at `release/DuckDuckWash Setup 1.0.3.exe` — now includes wearer collapse sections, per-wearer spreadsheet presets, and Reports xlsx export.
- **v1.0.4 built 2026-06-16** (installer 104.8MB, SHA256 2FE8229B…, 17:33) at `release/DuckDuckWash Setup 1.0.4.exe`, version bumped. Branch `feat/v1.0.4-aunt-feedback` (subagent-driven). Aunt feedback round 2 — 5 features (import .xlsx deliberately deferred, exported Orders sheet is summary-only/lossy):
  1. **Edit order date on existing order** — OrderDetails has a 📅 date picker (`max=today`) loaded from `created_at`; `saveDetails` accepts optional `created_at`, updates via `created_at=COALESCE(?, created_at)` (sent only when changed). Closed orders can't be edited (saveDetails rejects closed).
  2. **SOCKS half-step (.5)** — no DB migration (SQLite INTEGER affinity keeps 0.5 as REAL). Row where garment name = `socks` (case-insensitive) gets `step=0.5 min=0.5`, no floor; others stay integer. `filledGarments` threshold `>=1`→`>0`; saveDetails guard `<1`→`<=0`.
  3. **Per-section "Other" input** — moved the add-clothing-type input from one shared top box into each wearer collapse (draft state `Record<Wearer,string>`, `addSectionType`); added type still shows across all sections via existing `addExtras`.
  4. **Iron default price 40, editable** — `db.ts` seeds iron `default_price=40` + migration `UPDATE services SET default_price=40 WHERE key='iron' AND default_price IS NULL`. Still editable per order (pricing stays `custom`).
  5. **Urgent surcharge** — flat baht amount (changed from % 2026-06-19): `orders.surcharge_amount REAL NOT NULL DEFAULT 0`. `computeOrderTotal(items, surchargeAmount=0)` = `subtotal + amount`. OrderDetails "Urgent surcharge (฿)" field; total box shows `subtotal + amount ฿ urgent` breakdown when >0. Aunt types baht directly (e.g. +100). **Was percentage in v1.0.4/v1.0.5** (`surcharge_pct`, `subtotal*(1+pct/100)`); aunt wanted a number not %. Column renamed via `renameColumn('orders','surcharge_pct','surcharge_amount')` migration in db.ts (any existing pct values become baht as-is — negligible, single user, orders editable). 18/18 tests + typecheck green.
  - Spec `docs/superpowers/specs/2026-06-16-v1.0.4-aunt-feedback-design.md`, plan `docs/superpowers/plans/2026-06-16-v1.0.4-aunt-feedback.md`.
  - Verified: 18/18 vitest green (added surcharge math, surcharge_pct column, iron-migration tests), typecheck node+web clean, build:win clean. This is the build to hand to aunt.
- **v1.0.4 in progress** on branch `feat/v1.0.4-aunt-feedback`. Backend (types/IPC) already done in prior session. UI Task 6 sub-tasks completed 2026-06-16:
  - **6a** (commit `68a574c`): urgent surcharge % field + total breakdown on OrderDetails. `surchargePct` state; saved via `saveDetails.surcharge_pct`; subtotal/total split with conditional breakdown line.
  - **6b** (commit `6d16ddc`): editable order date on OrderDetails. `orderDate` state initialized from `r.order.created_at`; date picker with `max=today`; saves `created_at` only when changed.
  - **6c** (commit `cf476b9`): SOCKS allow 0.5 quantity. `isSocks` derived from garment name; `filledGarments` threshold changed to `> 0`; step/min 0.5 for socks rows.
  - **6d** (commit `6d59129`): per-section "Other" input replaces shared top-level input. `drafts: Record<Wearer,string>` state; `addSectionType(w)` function; each wearer section has its own input+button. Cross-section behavior preserved (addExtras still adds to extraTypes shown everywhere).

### v1.0.5 — startup-crash fix + chart fix + auto-updater (2026-06-18, branch `fix/v1.0.5-startup-crash-chart-updater`)
- **ROOT CAUSE of "v1.0.4 won't open" (aunt: double-click shortcut, nothing opens, not in Task Manager):** packaged `better_sqlite3.node` was built for **node ABI (NODE_MODULE_VERSION 137)** but Electron 39 needs **140**. `openDb()` threw at `new Database()` → unhandled rejection → no window. The "ABI seesaw" bit: at v1.0.4 build time `node_modules` held the node-ABI binary (from a prior `npm test`/`rebuild:node`) and `electron-builder` packaged it without rebuilding to Electron ABI. **Reproduced** by launching `release/win-unpacked/DuckDuckWash.exe` directly — every prior "verification" was tests+typecheck+"build clean", the packaged exe was NEVER launched.
  - **Fix 1 (prevent):** `build:win` script now starts with `npm run rebuild:electron` so the Electron-ABI binary is always what gets packaged. (electron-builder's own @electron/rebuild also runs, now belt-and-suspenders.)
  - **Fix 2 (never fail silently):** `src/main/index.ts` wraps `openDb()` in try/catch → `dialog.showErrorBox(...)` + `app.quit()`. A non-technical user now sees a reason instead of a dead double-click.
  - **Verified:** rebuilt `release/DuckDuckWash Setup 1.0.5.exe`, launched win-unpacked exe → window opens, no ABI error.
- **Revenue chart fix** (`Reports.tsx`): bars used `height: %` but their direct parent wrapper had no defined height (only the `h-48` grandparent did) → every bar collapsed to the 4px `minHeight` stub. Changed track `items-end`→`items-stretch` and wrapper to `... justify-end` so the wrapper fills `h-48` and `%` resolves. CSS-only, no IPC/data change. Bug was version-independent (always broken since range-report chart `92bbc15`).
- **Auto-updater added (electron-updater + GitHub Releases):** `electron-updater@^6` dep; `src/main/index.ts` calls `autoUpdater.checkForUpdatesAndNotify()` on ready (production only, `.catch`-guarded) — silent background download, install on quit (no prompts for aunt). `electron-builder.yml` `publish: null` → `provider: github, owner: YOUR_GITHUB_USERNAME, repo: duckduckwash`. **Motivation:** aunt keeps asking for features; updater lets us push versions without re-sending installers. Reverses the old "auto-update declined" decision.
- **SHIPPED 2026-06-18:** merged to `main` (ff), pushed to **public repo `github.com/peerapongsm/duckduckwash`** (GitHub login is `peerapongsm`, NOT the display name "Peerapong Saksommon" — `owner` must be the login). Published **GitHub Release `v1.0.5`** (draft=false): assets `DuckDuckWash-Setup-1.0.5.exe` (110MB) + `.blockmap` + `latest.yml`, verified publicly reachable at `/releases/latest/download/latest.yml`. Auto-updater feed is live.
  - **Gotcha for future releases:** `electron-builder --win --publish always` created the GitHub release as a **draft** and the 110MB exe + `latest.yml` upload didn't complete (only `.blockmap` landed). Had to: `cp "DuckDuckWash Setup 1.0.5.exe" "DuckDuckWash-Setup-1.0.5.exe"` (latest.yml expects the **dashed** name), `gh release upload v1.0.5 <dashed-exe> latest.yml --clobber`, then `gh release edit v1.0.5 --draft=false`. electron-updater ignores drafts — always un-draft. Consider adding `releaseType: release` to the publish config to skip the draft step.
  - **Only remaining step:** install v1.0.5 on aunt's PC once (first unsigned install may still hit SmartScreen — "More info → Run anyway"); subsequent updates auto-install on quit.
  - **To push a new feature going forward:** bump version → `npm run build:win` → publish exe+latest.yml to a new (non-draft) GitHub Release. Aunt's app self-updates.
- **Found, not fixed (out of scope):** bundled Fredoka/Nunito fonts (`data:font` URIs) are blocked by renderer CSP `default-src 'self'` → app falls back to system fonts. Cosmetic.

### v1.0.6 — surcharge flat baht (2026-06-19, shipped on `main`)
- Only change: urgent surcharge % → flat baht (see Feature 5 above). Commits `990ec93` (feature) + `041fc28` (version bump).
- **SHIPPED via auto-updater:** built `release/DuckDuckWash Setup 1.0.6.exe` (110MB), **launched `release/win-unpacked/DuckDuckWash.exe` directly → window opens, alive 8s, no ABI crash** (the v1.0.4 verification gap — always launch the packaged exe). Published non-draft GitHub Release `v1.0.6` with dashed `DuckDuckWash-Setup-1.0.6.exe` + `.blockmap` + `latest.yml`; verified draft=false, assets full size, `/releases/latest/download/latest.yml` public. **Aunt confirmed on v1.0.5 (installed) — so she gets v1.0.6 fully automatically** (updater downloads in background, installs on quit). No more manual installer sends; every future release just needs build + non-draft GitHub Release.
- Release recipe worked clean this time: `gh release create v1.0.6 <dashed-exe> <blockmap> latest.yml --latest` (gh defaults to non-draft, avoids the v1.0.5 draft+incomplete-upload gotcha).

### v1.0.7 — edit expenses for any month (2026-06-23, branch `claude/gifted-fermat-mblz6g`)
- **Problem (aunt):** could only see/manage the *current* month's expenses. The Expenses screen hard-coded `month = today.slice(0,7)` with no way to reach older months, so past-month expenses were invisible and effectively uneditable.
- **Fix (`Expenses.tsx`, renderer-only):** `month` is now React state with a ← / → navigator showing the full month label (e.g. "June 2026"). ← goes back arbitrarily far; → is disabled on the current month (no future months). List + add + delete all operate on the viewed month. The add-expense date picker is bounded to the viewed month (`min`/`max` via `monthBounds`) so a new expense always lands in the month on screen — defaults to today for the current month, else the 1st. Empty state shows the month name. **No DB/IPC/types change** — existing `expenses.list(monthPrefix)` / `createMany` / `remove` already accept any date; the only barrier was the locked-to-current-month UI.
- Verified: `npm run typecheck` (node+web) clean, `electron-vite build` clean. Tests untouched (pure UI change; better-sqlite3 native binary not rebuilt in this Linux cloud env, so `npm test` not run here).
- **`electron-builder.yml`:** added `publish.releaseType: release` so future releases publish non-draft directly (kills the v1.0.5 draft gotcha this log had flagged).
- **CI release workflow added (`.github/workflows/release.yml`):** builds the installer on `windows-latest` and publishes via `electron-builder --win --publish always` (new `release:win` script). Triggered by a `v*` tag push OR `workflow_dispatch`. Runs `rebuild:electron` (Electron-ABI better-sqlite3) + a `typecheck` gate first. Removes the manual Windows-build bottleneck — no local Windows PC needed to ship. Motivation: this session ran in a Linux cloud container (no Wine/native toolchain), so it couldn't `build:win` locally.
- **Gotcha — dispatch needs a tag:** first dispatch (run #1, on `main`) built fine but FAILED at publish with `422 "Published releases must have a valid tag"`, because `releaseType: release` (non-draft) requires the git tag to already exist and a `workflow_dispatch` run has no tag (unlike a tag push). **Fix (commit `20c03aa`):** a "Create version tag if missing" step does `git tag v$version && git push` from the runner — idempotent, and uses `GITHUB_TOKEN` which does NOT retrigger `on:push:tags` (no loop). Normal tag-push releases skip it.
- **Dispatch-from-branch trick:** once the workflow is registered on the default branch (`main`, via merged PR #1), `workflow_dispatch` can target ANY ref and runs that ref's copy of the workflow — so the fix was validated by dispatching `ref=claude/gifted-fermat-mblz6g` without pushing to `main`. (Sandbox also blocks tag pushes + main pushes with 403 → had to dispatch, not tag.)
- **SHIPPED 2026-06-23 via CI (run #2):** GitHub Release **v1.0.7** published non-draft by `github-actions[bot]` — assets `DuckDuckWash-Setup-1.0.7.exe` (110MB, sha256 fa65a51f…) + `.blockmap` + `latest.yml`, consistently dashed (electron-builder self-publish → no manual rename, unlike the v1.0.5 gotcha). `get_latest_release` = v1.0.7 → `/releases/latest/download/latest.yml` live → aunt (on v1.0.5/v1.0.6) auto-updates on next quit.
- **NOT launch-verified:** nobody ran the packaged v1.0.7 exe (CI + Linux session both can't). ABI crash class is structurally prevented (rebuild:electron in CI), but the project's "always launch the packaged exe" rule was not satisfied — download the exe + double-click to confirm if paranoid; delete the release if broken.
- **TODO future releases:** `main`'s `release.yml` still lacks the tag-creation fix (it lives on the feature branch, not yet re-merged to main). Tag-push releases on main work as-is (tag exists); `workflow_dispatch` releases on main will 422 until the feature branch is merged to main again. Re-merge to keep both paths working. **(RESOLVED in v1.0.8 — branch re-merged to main, see below.)**

### v1.0.8 — filter orders by date range (2026-06-23, branch `claude/gifted-fermat-mblz6g`)
- **Feature:** Orders screen gained a date-range filter row under the status tabs — presets **All dates** (default, empty bounds = no filter) / Today / This month / This year + free from/to date pickers + Clear. Filters within the active status tab and persists across tab switches. `start>end` shows a message; filtered-empty says "No orders in this date range". UX mirrors the Reports range filter for consistency.
- **Backend:** extracted the inline `orders:list` query into a testable pure fn `listOrders(db, status, from?, to?)` in `src/main/logic/orders.ts` (matches the `logic/` + `rangeReport` convention). Filters on `date(created_at)` (order's calendar day, time ignored); either bound optional → open-ended ranges; both omitted → all. `orders:list` IPC + preload `orders.list` gained optional `from`/`to` — backward compatible (old single-arg callers unaffected).
- Verified: **22/22 vitest green** (new `tests/orders.test.ts`: no-range newest-first, inclusive range ignoring time, open-ended from-only/to-only, status isolation), typecheck node+web clean, `electron-vite build` clean. **better-sqlite3 node-ABI rebuild worked in this Linux cloud env this time** (`npm run rebuild:node` → `npm test`), unlike the v1.0.7 session — tests actually ran here.
- **Re-merged feature branch → `main`:** brings the orders filter + the v1.0.7 `release.yml` **tag-creation fix** + the 1.0.8 bump onto `main`, so `main`'s workflow now supports BOTH tag-push and `workflow_dispatch` releases (resolves the v1.0.7 TODO above).
- **Shipped via CI run #3 — but BROKEN by a duplicate-release race (fixed in v1.0.9):** the run went green, but electron-builder uploads assets concurrently and each missing-release upload creates the release → it raced into **two non-draft releases for tag `v1.0.8`**: id `343286342` got `exe`+`latest.yml` (what `get_latest_release` returns), id `343286341` got only `.blockmap`. GitHub's tag download alias resolved to the blockmap-only release, so `releases/latest/download/latest.yml` and `releases/download/v1.0.8/<exe>` returned **404** (verified via curl) → electron-updater can't fetch the feed → **aunt would NOT have gotten v1.0.8.** Superseded by v1.0.9. **Lesson: after every release, curl `releases/latest/download/latest.yml` and expect 200 — a green workflow is not proof the feed works.**

### v1.0.9 — fix duplicate-release race (2026-06-23, branch `claude/gifted-fermat-mblz6g`)
- **Problem:** v1.0.8 produced two GitHub releases for one tag (electron-builder concurrent uploads each create-if-missing → race), splitting `latest.yml`/`exe`/`blockmap` and 404-ing the updater feed (see v1.0.8 above).
- **Fix (`release.yml`):** new **"Pre-create GitHub Release"** step (after the tag step, before build) runs `gh release create v$version` when the release doesn't yet exist, so electron-builder finds the single existing release and uploads all 3 assets to it — no concurrent creation, no duplicate. Idempotent (skips if present); covers tag-push + dispatch flows. `gh` is preinstalled on windows-latest, auth'd by `GH_TOKEN`.
- **No app-code change** — v1.0.9 == v1.0.8 features (orders date-range filter) + the workflow race-fix + version bump. Re-released (not patched) because the broken v1.0.8 releases can't be deleted via the available GitHub MCP tools (no delete/edit-release or asset-upload); v1.0.9 supersedes for the updater (newest version wins).
- **Cleanup TODO (owner, manual in GitHub UI):** delete the two stray `v1.0.8` releases (ids `343286341` + `343286342`) and optionally the `v1.0.8` tag — harmless clutter once v1.0.9 is latest.
- **Release + GATING CHECK:** dispatched on `main` via the fixed workflow → single clean `v1.0.9` release. Not called shipped until `curl -sL releases/latest/download/latest.yml` returns **200 + valid yml** (the exact check v1.0.8 failed). Not launch-verified (CI/Linux can't run the exe; ABI crash class prevented by `rebuild:electron`).

### v1.1.0 — revenue & expense breakdowns on Reports (2026-06-23, branch `claude/gifted-fermat-mblz6g`)
- **Feature (aunt request — "revenue breakdown like expense breakdown"):** Reports screen shows two breakdown panels under the Revenue/Expenses/Profit cards: **Revenue by service** (wash_dry_fold / wash_dry_fold_iron / iron / dry_clean, summed from `order_items.total`, + an **Urgent surcharge** line from `orders.surcharge_amount` so the slices reconcile to total revenue) and **Expenses by category** (supplies/utilities/rent/food/salary/other). Each row = ฿ amount + share % + proportional bar; sorted desc, zero slices hidden; both respect the selected date range.
- **Backend (`logic/reports.ts`):** `rangeReport` now also returns `revenueByService` + `expensesByCategory` (`BreakdownItem[] = {label, amount}`, added to `RangeReport`), via grouped queries within `date BETWEEN from AND to`. Display labels centralised in new **`src/shared/labels.ts`** (`SERVICE_LABELS` + `EXPENSE_CATEGORY_LABELS`); `reportExport.ts` now imports SERVICE_LABELS from there (de-duped). Revenue-by-service reconciles to headline revenue except the rare legacy v1.0.0 delivery +20 (negligible — fresh data, delivery now free).
- **Renderer (`Reports.tsx`):** new `BreakdownPanel` component (label/฿/%/bar); two panels in a `md:grid-cols-2` grid between the summary cards and the over-time chart.
- Verified: **23/23 vitest** (new breakdown test asserts slices reconcile to revenue + range filtering), typecheck node+web clean, `electron-vite build` clean. Not GUI-run (no display in Linux env).
- **Minor bump 1.0.9 → 1.1.0** (first feature minor). Shipped via CI (pre-create-release workflow from v1.0.9). Gating check: `curl releases/latest/download/latest.yml` must be 200.

## 4. Notes
- User invokes /memory-first each session; works in caveman+pordee terse mode.
- **Next step: deliver v1.0.1 to aunt.** Distribution: MediaFire blocked by SmartScreen + Chrome Safe Browsing (unsigned exe, zero reputation, bad-rep file host). Advice given: share via Google Drive/OneDrive or USB; first run needs SmartScreen "More info → Run anyway". Code signing cert deemed overkill for one user.
- Known deferred items: assistant role w/ PIN (Reports hidden), auto-start on Windows boot (`app.setLoginItemSettings`) — user asked about "startup" once, clarified as Start Menu shortcut, boot-launch explicitly not done yet.
- If logo changes again: replace `icon/duckduckwash.png`, then re-copy to `src/renderer/src/assets/logo.png` + `resources/icon.png` and regenerate square `build/icon.png`, then `npm run build:win`.
