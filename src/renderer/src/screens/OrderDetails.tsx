import type { JSX } from 'react'
import type { Screen } from '../App'
export default function OrderDetails({ orderId: _orderId, go: _go }: { orderId: number; go: (s: Screen) => void }): JSX.Element { return <div /> }
